<!DOCTYPE html>
<html>
  <head>
    <title>RailsBlog</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  <body>
    <header>
      <div class="header-logo"><%= link_to 'Rails Blog', '/top' %></div>
      <div class="header-list">
        <ul>
          <li><%= link_to 'トップ', '/top' %></li>
          <% @menus.each do |menu| %>
            <li><%= link_to menu.menu_name, "/#{menu.menu_url}/index" %></li>
          <% end %>
            <li><%= link_to '+', '/new_category' %></li>
        </ul>
      </div>
    </header>
    <div class="top-title">
      <div class="title-container">
        <h1 class="top-link"><%= link_to "Rails Blog", '/top' %></h1>
        <p class="sub-title">〜Ruby on Rails を使用して、丹精込めて作り上げました〜</p>
      </div>
    </div>
    <div class="category">
      <ul>
        <li>
          <span class="base-text">トップ</span>
          <span class="change-text"><%= link_to("top", "/top") %></span>
        </li>
        <% @menus.each do |menu| %>
          <li>
            <span class="base-text"><%= menu.menu_name %></span>
            <span class="change-text"><%= link_to menu.menu_url, "/#{menu.menu_url}/index" %></span>
          </li>
        <% end %>
        <li><%= link_to '+', '/new_category' %></li>
      </ul>
    </div>
    <% if flash[:notice] %>
      <%= flash[:notice]%>
    <% end %>
    <%= yield %>
  </body>
</html>
